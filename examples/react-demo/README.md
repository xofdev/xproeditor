# @xproeditor/react demo

Runnable Vite + React app showing `<ProEditor>` with a mode switch between the
fixed toolbar and the Notion-like floating toolbar/slash-menu experience.

```bash
npm install
npm run dev -w react-demo
```
