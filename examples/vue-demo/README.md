# @xproeditor/vue demo

Runnable Vite + Vue 3 app showing `<ProEditor>` with a mode switch between the
fixed toolbar and the Notion-like floating toolbar/slash-menu experience.

```bash
npm install
npm run dev -w vue-demo
```
